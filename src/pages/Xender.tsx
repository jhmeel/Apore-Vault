import React from 'react';
import { styled } from '@mui/material/styles';
import {
  Modal,
  Box,
  Typography,
  Grid,
  Button,
  Divider,
  TextField,
  InputAdornment,
} from '@mui/material';
import { Ioffering } from '../types';
import MainTxModal from '../components/MainTxModal';

const Xender = () => {
 
  return (
  <Box>
Xend
  </Box>
  );
};

export default Xender;